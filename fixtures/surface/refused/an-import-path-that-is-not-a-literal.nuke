# `@import` takes a string literal, so that what a file imports is a property of
# its text and a tool can read the graph without evaluating it. Neither direction
# of the operator changes that.
path := "../modules/palette.nuke"

path |> @import
