# A float has more than one canonical spelling — `1e5` and `100000.0` are both one
# — and the canonical form gives that choice to the formatter. A hole cannot make
# it early, so it refuses and the text is written as text.
opacity := 0.9

$"opacity {opacity}"
