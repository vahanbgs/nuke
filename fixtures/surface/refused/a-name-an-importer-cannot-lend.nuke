# And a module sees none of the importer's bindings either. Privacy runs both
# ways, because each file is reduced in a scope of its own.
secret := "#282828"

[secret @import <| "../modules/needs-a-name.nuke"]
