# Access is postfix and reads left to right, so what it yields is projectable
# again.
theme := {editor = {colors = {accent = "#FE8019"}}}

{
	colors = theme.editor.colors
	accent = theme.editor.colors.accent
}
