# A comment stands wherever whitespace does, a binding adds two positions, and a
# hole's interior is ordinary text, so one stands there too.
{
	# above the binding
	n # after the name
	:= # after the binder
	1 # after the value

	a = n
	b = $"{n # in a hole
	}"
}
