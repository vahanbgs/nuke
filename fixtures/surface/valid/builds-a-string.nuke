# `@concat` is the first builtin about values rather than files, which is what
# makes `@` a namespace. An application takes one argument, so the parts arrive
# as a list however many of them there are.
prefix := "#"

[
	@concat <| [prefix "1D2021"]
	["a" "b" "c"] |> @concat
	@concat <| ["only"]
]
